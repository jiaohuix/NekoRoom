请排查以下测试反馈问题：

1. 模型推理异常：
当前下载默认 Q4 FP16 embedding 模型后，无论输入什么内容，模型始终只回复固定文本：
“你好，我有什么可以帮助你的吗？”

请重点检查：
- 模型加载是否正确
- chat template 是否匹配
- conversation history 是否被缓存污染
- KV cache 是否复用错误
- sampler / decode 参数是否异常

需要定位根因，不要简单增加随机性。

2. TTS播放异常：
语音播放过程中偶尔出现短暂音色闪变/卡顿。

请检查：
- streaming chunk 拼接
- 音频 buffer
- 播放队列切换
- 是否存在不同音频片段采样率/格式不一致

目标：
保证连续播放无明显断层。

3. 性能统计优化：
当前性能参数统计逻辑需要修改。

要求：
- 只统计最近3次记录
- 不满3次时，使用已有次数平均
- 不做加权平均

例如：
1次 -> 使用1次
2次 -> 平均2次
3次以上 -> 最近3次平均

4. 模型名称UI优化：
当前名称过长,得优化展示：
MiniCPM5-1B-Q4
MiniCPM5-1B-Q4-Embed8
MiniCPM5-1B-Q4-Embed4

内部真实名称不要修改，只优化显示。

5. 开发者模式UI问题：
当前开发者模式控件过多，导致TTS区域无法正常操作。

问题：
- 下方区域被挤压
- 滑动区域自动收起
- TTS按钮无法点击

请重新调整布局：
- 保证所有调试项可访问
- 避免自动折叠影响操作
- 优化开发者模式信息密度

完成后：
- 编译验证
- 如果设备连接，测试相关功能
- 每个独立问题提交一个commit